cd mobileui
./gradlew :app:assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell pm grant com.smsjustu.app android.permission.SEND_SMS   # one time

# One-shot send via adb - the app's SendSmsReceiver, no UI or sync loop needed.
adb shell "am broadcast -f 0x00000020 \
  -n com.smsjustu.app/.SendSmsReceiver \
  --es number '16505551234' \
  --es message 'hello from adb'"

# Optional --ei subId <n> to pick a SIM on a dual-SIM device.
# Success prints: Broadcast completed: result=-1, data="sent 1 part(s)"
# Driven in a loop by _script/autoscript.py.
